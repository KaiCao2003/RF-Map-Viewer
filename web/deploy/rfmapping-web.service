[Unit]
Description=RF Mapping Web App
Documentation=file:/srv/rfmapping/current/deploy/README.md

[Service]
Type=simple
WorkingDirectory=/srv/rfmapping/current
EnvironmentFile=/srv/rfmapping/shared/rfmapping-web.env
EnvironmentFile=/home/rfmapping/.config/lab-access-gate/pi-first-name.env
Environment=PYTHONDONTWRITEBYTECODE=1
Environment=PYTHONUNBUFFERED=1
Environment=TMPDIR=/srv/rfmapping/tmp/runtime

ExecStartPre=/usr/bin/test -x /home/rfmapping/.virtualenvs/rfmapping/bin/python
ExecStartPre=/usr/bin/test -r /srv/rfmapping/current/web/dist/index.html
ExecStartPre=/usr/bin/test -d /srv/rfmapping/cache
ExecStartPre=/usr/bin/test -d /srv/rfmapping/exports
ExecStartPre=/usr/bin/test -w /srv/rfmapping/exports
ExecStart=/home/rfmapping/.virtualenvs/rfmapping/bin/python -m uvicorn rfmapping_web.app:app --host 127.0.0.1 --port 3005 --proxy-headers --forwarded-allow-ips=127.0.0.1

Restart=on-failure
RestartSec=3s
TimeoutStartSec=60s
TimeoutStopSec=30s
KillSignal=SIGINT
UMask=0027

# This is a user unit so releases can restart without sudo. rfmapping_remote's user
# manager cannot apply mount-namespace directives. Dataset routes remain
# read-only; figure publication is confined to the separately configured export
# root, existing directories, known extensions, and explicit overwrite requests.
NoNewPrivileges=true
RestrictSUIDSGID=true
LockPersonality=true
RestrictAddressFamilies=AF_UNIX AF_INET AF_INET6

[Install]
WantedBy=default.target
