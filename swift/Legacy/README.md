# Legacy Swift source

`StackView.swift` is preserved from the former remote monolith for reference.
The current viewer no longer exposes the old Stack tab, so this directory is
outside `Sources/` and is intentionally not compiled by Swift Package Manager.
Port the view to the current models before moving it into an active target.
